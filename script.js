const drawer=document.querySelector('.cart-drawer');const overlay=document.querySelector('.overlay');const countEl=document.querySelector('.cart-count');const itemsEl=document.querySelector('.cart-items');const emptyEl=document.querySelector('.cart-empty');const totalEl=document.querySelector('.cart-total');let cart=[];function openCart(){drawer.classList.add('open');overlay.classList.add('open');drawer.setAttribute('aria-hidden','false')}function closeCart(){drawer.classList.remove('open');overlay.classList.remove('open');drawer.setAttribute('aria-hidden','true')}function renderCart(){countEl.textContent=cart.length;itemsEl.innerHTML=cart.map((item,i)=>`<div class="cart-item"><span>${item}</span><button type="button" data-remove="${i}" style="border:0;background:none;cursor:pointer">×</button></div>`).join('');emptyEl.style.display=cart.length?'none':'block';totalEl.textContent=cart.length?`${cart.length*1490} ₴`:'0 ₴'}document.querySelectorAll('[data-action="cart"]').forEach(b=>b.addEventListener('click',openCart));document.querySelector('[data-close="cart"]').addEventListener('click',closeCart);overlay.addEventListener('click',closeCart);document.addEventListener('click',e=>{const add=e.target.closest('[data-add]');if(add){cart.push(add.dataset.add);renderCart();openCart()}const remove=e.target.closest('[data-remove]');if(remove){cart.splice(Number(remove.dataset.remove),1);renderCart()}});document.querySelectorAll('a[href="#"]').forEach(a=>a.addEventListener('click',e=>e.preventDefault()));document.getElementById('newsletterForm').addEventListener('submit',e=>{e.preventDefault();const input=e.currentTarget.querySelector('input');if(input.value){e.currentTarget.innerHTML='<p style="margin:0;padding:15px 0;font-weight:700;letter-spacing:.1em;text-transform:uppercase;font-size:10px">Дякуємо. Ви в списку.</p>'}});renderCart();